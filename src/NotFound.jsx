import PropTypes from "prop-types";

/*TODO: link "learn more" to about*/
const NotFound = () => {
    return (
        <div>
            <p>Not found</p>
        </div>
    );
};

NotFound.propTypes = {
}

export default NotFound;